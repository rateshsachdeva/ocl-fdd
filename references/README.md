# References

- `source/`: client/raw source area when needed for evidence and controlled fallback.
- `other/`: engagement context such as call notes or methodology documents.

Normal OCL processing should consume the approved standardized publication from
`fdd-data-preparation` rather than independently rebuilding raw client files.
Client and engagement files are ignored by Git.
